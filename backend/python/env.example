# Environment Variables Example File
# Copy this file to `.env` in the project root and adjust values as needed.
# The Flask application reads these variables using python-dotenv.

# Redis connection URL for caching.
# Format: redis://[username:password@]host:port/database
# If Redis is running locally with default settings, use:
REDIS_URL=redis://localhost:6379/0

# Additional environment variables can be added here as needed,
# for example: API keys, secret keys, debug flags, etc.
# FLASK_ENV=development
# SECRET_KEY=your-secret-key